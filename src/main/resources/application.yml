server:
  port: 8090
#  servlet:
#    context-path: /api/v1

spring:
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/animalbrokers
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create